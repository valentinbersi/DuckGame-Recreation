#include "EquippableItem.h"

// EquippableItem::EquippableItem(GunID id, GameObject* parent, Vector2 position, const float
// rotation,
//                std::unique_ptr<Shape2D> shape, Vector2 initialVelocity):
//         PhysicsObject(parent, position, rotation, 4, 2, std::move(shape), initialVelocity,
//                       Gravity::Enabled),
//         gunID(id) {}

// EquippableItem::~EquippableItem() = default;